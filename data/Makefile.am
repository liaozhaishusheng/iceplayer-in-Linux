SUBDIRS = skin

desktopdir = /usr/share/applications
desktop_DATA = $(PACKAGE).desktop

pngdir = /usr/share/$(PACKAGE)
png_DATA = $(PACKAGE).png $(PACKAGE)_tray.png

EXTRA_DIST = $(desktop_DATA) $(png_DATA)
